spring.application.name=airBnbClone

# Database connection properties
spring.datasource.url=jdbc:postgresql://localhost:5432/airbnb_clone
spring.datasource.username=postgres
spring.datasource.password=root

# JPA (Hibernate) properties (if using Spring Data JPA)
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# SQL initialization (REQUIRED for data.sql)
spring.sql.init.mode=always
spring.jpa.defer-datasource-initialization=true

#base path
server.servlet.context-path=/api/v1

#swagger
springdoc.swagger-ui.path=/swagger.html

#jwt
jwt.secretKey = qwerioopisfdkjafkj87sas8098sajkkjkafmnmnvxlkjwrwrklj

#Payment Gateway
frontend.url=http://localhost:8080
stripe.secret.key=sk_test_xyz
stripe.webhook.secret=whsec_abc
